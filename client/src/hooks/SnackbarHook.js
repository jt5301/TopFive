import { useState, useEffect } from 'react'
import Snackbar from '@material-ui/core/Snackbar';

export const useSnackbar = (bool, message) => {
  const [Message, setMessage] = useState(false)

  const closeSnackbar

}
